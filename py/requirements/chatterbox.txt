# Chatterbox is installed by the Rust backend manager with:
#   uv pip install chatterbox-tts==0.1.7 --no-deps
# Keep torch/torchaudio owned by the selected backend pack.
numpy>=1.26,<2
librosa==0.11.0
s3tokenizer
huggingface_hub>=0.34,<1
# Chatterbox declares transformers==5.2.0, but qwen-tts 0.1.1 requires
# 4.57.3 and both adapters import cleanly on 4.57.3 in the shared backend.
transformers==4.57.3
accelerate==1.12.0
diffusers==0.29.0
resemble-perth @ git+https://github.com/resemble-ai/Perth.git@master
conformer==0.3.2
safetensors==0.5.3
spacy-pkuseg
pykakasi==2.3.0
pyloudnorm
omegaconf
